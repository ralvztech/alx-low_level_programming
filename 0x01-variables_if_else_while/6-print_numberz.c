#include <stdio.h>
/**
 * main - Print single digit number of base 10
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	in c;

	c = a;
	while
		(c < 10) {
			putchar('c';
			c++;
		}
	putchar('\n');
	return (0);
}
