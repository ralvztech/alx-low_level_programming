#include <stdio.h>
/**
 * main - A program to print single digit of base 10 from 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
